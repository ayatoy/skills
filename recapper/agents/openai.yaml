interface:
  display_name: "Recapper"
  short_description: "Summarize the current session into $PWD/docs/notes, analyze repeated work patterns, and recommend Agent Skills"
  default_prompt: "Use $recapper to summarize the current session so far into a detailed note under $PWD/docs/notes with a yyyy-MM-dd filename prefix, append an analysis of repeated work patterns observed in the session, and recommend any recurring patterns that should become Agent Skills."
